#include <iostream>

#include "Propagator/Electron.h"
#include "Propagator/Phonon.h"
#include "Vertex/Vertex.h"
 
int main()
{
  // Electron g1(Vector3d(1,2,3)), g2(Vector3d(3,2,1));
  Phonon d1(Vector3d(2,3,4),1, 1);
  // Vertex v1(1), v2(2);

  // g1.setStart(&v1);
  // g1.setEnd(&v2);

  // d1.setStart(&v1);
  // d1.setEnd(&v2);
  // d1.setStart(&v2);


  // g1.print();
  // g2.print();
  // d1.print();

  // v1.print();
  // v2.print();

}